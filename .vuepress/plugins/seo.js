module.exports = ['seo', {
  // image: ($page, $site) => $page.frontmatter.image && (($site.themeConfig.domain || '') + $page.frontmatter.image),
  // image: ($page, $site) => ($site.themeConfig.domain || '') + $site.themeConfig.logo,
}]